<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_report_recurring_tasks" model="ir.actions.report">
        <field name="name">Recurring Tasks Summary</field>
        <field name="model">recurring.task.template</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">lv_nova_recurring_tasks.report_recurring_tasks</field>
        <field name="report_file">lv_nova_recurring_tasks.report_recurring_tasks</field>
        <field name="print_report_name">'Recurring_Tasks_%s' % object.name.replace(' ','_')</field>
        <field name="binding_model_id" ref="model_recurring_task_template"/>
        <field name="binding_type">report</field>
    </record>

</odoo>
